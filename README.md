# Orchestra React — Hybrid Theme (Black + Gold + Logo Accents)
Cinematic black + gold base with subtle accents from your logo.

## Languages
EN (default), AL, MK — switch from the 🌐 dropdown (flags + text).

## Run
```bash
npm install
npm run dev
```
